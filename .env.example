# Local secrets — never commit. Copy to `.env` and fill in.
#
# WARROOM_LLM: replay (default for CI/demo) | gemini | auto (gemini if key present)
WARROOM_LLM=gemini
GEMINI_API_KEY=
# Optional override of the Generative Language API base URL
# GEMINI_API_BASE=https://generativelanguage.googleapis.com/v1beta
